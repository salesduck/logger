{
    "name": "@salesduck/logger",
    "version": "1.0.0",
    "main": "build/index.js",
    "types": "build/index.d.ts",
    "license": "MIT",
    "files": [
        "build",
        "!**/*spec.*"
    ],
    "scripts": {
        "clean": "rm -rf build && rm -rf ./tsconfig.build.tsbuildinfo",
        "build": "yarn clean && tsc --project ./tsconfig.build.json && tscpaths -p ./tsconfig.build.json -s ./src -o ./build",
        "lint": "yarn eslint --fix ./src",
        "test": "jest",
        "test:watch": "jest --watch",
        "test:debug": "node --inspect-brk node_modules/.bin/jest --runInBand"
    },
    "dependencies": {
        "@salesduck/symbols-logs": "1.0.0",
        "@salesduck/transport-logs": "1.0.0",
        "@salesduck/format-logs": "1.0.0"
    },
    "devDependencies": {
        "standard-version": "^9.3.0",
        "@types/jest": "^26.0.23",
        "@types/node": "^20.4.1",
        "jest": "^26.6.3",
        "ts-jest": "^26.5.5",
        "tscpaths": "^0.0.9",
        "typescript": "^4.9.4"
    }
}
